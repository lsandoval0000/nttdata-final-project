spring:
  datasource:
    url: jdbc:tc:mysql:5.7.34://bank-account-savings-service
  jpa:
    hibernate:
      ddl-auto: create-drop
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:password@localhost:8761/eureka